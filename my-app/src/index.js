/*
    Remember that when you do components, it means to export the functions into components. 
*/

import React from "react"
import ReactDOM from "react-dom"
import "./index.css"

import App from "./app"

ReactDOM.render(<App />, document.getElementById("root"))
